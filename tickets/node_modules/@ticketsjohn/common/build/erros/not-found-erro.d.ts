import { CustomError } from "./custom-error";
export declare class NotFound extends CustomError {
    statusCode: number;
    constructor();
    serializeError(): {
        message: string;
    }[];
}
